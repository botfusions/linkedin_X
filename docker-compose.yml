services:
  botfusions-agent:
    build: .
    container_name: botfusions-agent
    restart: always
    env_file:
      - .env
    environment:
      - TZ=Europe/Istanbul
    volumes:
      - agent_data:/app/data

volumes:
  agent_data:
